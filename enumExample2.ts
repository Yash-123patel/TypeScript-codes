//const enum that will access using string litteral or name
const enum Colors{
    ORANGE,
    RED,
    GREEN,
    BLUE,
    BLACK,
    WHITE,
    BROWN,
    GRAY,
    PURPLE
}

console.log(Colors.BLACK);

//const enum directly inlined in compile time .
//const enum does not create any run  time object